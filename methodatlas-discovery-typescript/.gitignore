# Runtime dependency tree — generated by 'npm install' / 'npm ci'; never commit
node_modules/

# npm debug and cache artefacts
npm-debug.log*
.npm/

# esbuild and other tool caches
.cache/

# package-lock.json is intentionally tracked so that CI uses 'npm ci'
# for fully reproducible installs.
